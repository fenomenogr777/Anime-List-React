import { useEffect, useState } from 'react'
import { useDispatch, useSelector } from 'react-redux'
import { loadAnimesLocalStorage } from '../store'

function LocalStorage() {}

export default LocalStorage
